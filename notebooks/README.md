# Notebooks

Scratch space for exploration before code gets finalized into `src/`. Notebooks here are for figuring out an approach – final, reusable logic belongs in `src/`, not in a notebook

Notebooks are created as needed when actual questions come up during development