# UI
reflex==0.9.12          # pinned: 0.9 removed rx.Base and implicit setters,
                        # and rejects background upload handlers (docs/UI.md)

# DB
psycopg2-binary>=2.9    # pgvector columns are written as text, so the
                        # pgvector Python package is not needed

# Parsing
pdfplumber>=0.11
python-docx>=1.1
PyMuPDF>=1.24           # PDF fallback when pdfplumber finds no text layer,
                        # and image detection for the ATS parsability check

# NLP
spacy>=3.7              # tokenizer + PhraseMatcher only; extraction runs on
                        # spacy.blank("en"), so no trained pipeline is needed
                        # at runtime (see src/nlp/extract_entities.py)
sentence-transformers>=3.0

# HTTP (Adzuna search, company job-board APIs)
httpx>=0.27

# Data
numpy>=1.26
python-dotenv>=1.0

# Testing
pytest>=8.0

# Deliberately absent:
#   rank-bm25   -- BM25 is implemented directly; the library cannot score a
#                  document outside its indexed corpus, which is exactly the
#                  requirement here (src/matching/bm25_scorer.py)
#   scikit-learn, pandas, requests, sqlalchemy, anthropic
#               -- listed during scaffolding, never imported. The Anthropic
#                  rewrite feature is a stretch goal that has not been built.
